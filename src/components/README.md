# Your gallery components

This folder is empty on purpose. Your work goes here.

Build at minimum:

- `GalleryGrid.jsx`
- `CaptureCard.jsx`
- `FilterBar.jsx`
- `Lightbox.jsx`

Wire them into `src/App.jsx` (replace the stub inside `<main>`).

See the [WildWatch Student Build Guide](../../docs/wildwatch-student-guide.md):
§04 for the `CaptureCard` data shape and §07 for the Claude prompt.
